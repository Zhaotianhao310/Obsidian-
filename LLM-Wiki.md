# LLM-Wiki 知识库规范（Obsidian + Codex 专用）
基础范式源自 Andrej Karpathy LLM-Wiki，适配中文双链知识库

## 核心架构：两层分离（强制遵守）
1. `/raw` 原始素材目录【只读、不可修改】
所有导入原文、网页摘录、截图、草稿、原始转录文本全部放入此处。
一旦存入，禁止AI修改、删除、重命名、覆盖。人类仅新增，不回溯修改旧raw文件。

2. `/wiki` 结构化知识库【AI维护区】
存放精炼原子笔记、概念词条、知识汇总。
双向链接、内容整合、信息提炼全部在此完成。

> 分工规则：
> 人类：只负责往 raw 新增素材、最终审核内容。
> AI(Codex)：只允许读写 wiki/，绝对不能触碰 raw/。

## 一、链接强制规范
- **只允许使用 Obsidian 双链 [[页面名称]]**
- 禁止使用普通 Markdown [文字](链接) 内部跳转；
- 新建页面、补充关联内容必须主动建立双向双链；

## 二、页面规范
1. 笔记原子化：一个页面只描述**单一概念/实体**，禁止大杂烩超长文档；
2. 页面标题简洁统一，不要使用特殊符号、超长中文标题；
3. wiki内禁止流水账日记、零散碎想法，随笔草稿统一放在 raw；

## 三、内容产出规则
1. 全部输出使用**简体中文**；
2. 整理素材时保留信息溯源，必要时标注来源raw文件名；
3. 内容力求精炼结构化，多用列表、分层标题，减少冗余抒情文字；
4. 发现新旧信息互相矛盾时，标记冲突，等待人类确认，不要自行选择采信哪一方。

## 四、AI操作权限红线【最高优先级】
✅ 允许操作：
- 新增wiki页面、补充段落、添加双链、优化排版、拓展相关知识点
- 拆分过长笔记、补全缺失关联链接、修正笔误

❌ **未经询问绝对禁止**
- 删除任意wiki页面
- 批量重命名页面（会破坏双链）
- 全文大规模重写已有页面
- 一次性批量创建10条以上空白新笔记
- 修改raw文件夹内任何文件
- 擅自删除已存在的标签、人类建立好的双向链接

> 高危操作执行前，先向我输出方案，等待确认再执行。

## 五、人类手动编辑标记
如果我手动修改 wiki/ 内任意笔记，请在文档顶部保留标记：
> 🧑 人工修改备注：XXX
Codex识别到此标记，**不可无询问直接覆盖人工修改内容**。

## 六、定期知识库自检（lint任务）
收到指令执行知识库体检时，检查并整理以下问题：
1. 孤立页面（无入链）
2. 失效、不存在的双链
3. 内容互相冲突的词条
4. 体积过大、需要拆分的大笔记
5. 缺少关联链接的相关主题页面

执行大规模整理前，先输出整理计划。

## 七、通用行为准则
1. 修改遵循「微创改动」：只处理当前任务相关内容，不顺手大面积优化无关笔记；
2. 不确定我的意图时，主动提问，不要自行猜测；
3. 所有改动尽量增量补充，优先保留原有有效内容；
4. 不要擅自新增文件夹、改动库目录结构。
## 八、 wiki/ 目录归档与分类标准

当 AI 提炼或创建新笔记时，必须严格按照以下规则选定存放路径：

1. **芯片与单片机硬件概念** $\rightarrow$ 放入 `wiki/01-Hardware/芯片名/`
2. **操作系统、内核机制、线程调度** $\rightarrow$ 放入 `wiki/02-OS-Kernel/系统名/`
3. **协议栈、音视频库、硬件驱动库原理** $\rightarrow$ 放入 `wiki/03-Middleware-Drivers/库名/`
4. **API 函数速查、接口文档、代码片段手册** $\rightarrow$ 放入 `wiki/04-API-Guides/技术名-API/`
5. **新建笔记后**，必须检查对应的 MOC 文件（如 `wiki/00-Index/xxx-MOC.md`），并将新笔记的双链 `[[页面名称]]` 补充到索引集中。
## 九、 新技术栈拓展与受控动态扩容规范（受控拓展协议）

随着知识库的成长，当引入不属于现有五大子分类（如新增 `FPGA`、`Qt`、`ROS`、`CUDA` 等）的技术领域时，AI (Codex / Claude Code) 必须遵循以下“提案-确认”流程，**严禁自行默默创建新的文件夹**。

### 1. 新技术归类评估标准
当收到新素材或创建新笔记指令时，AI 须先按顺序评估：
1. **优先归入现有目录**：检查是否能归入现有子文件夹（如 `01-Hardware/`、`02-OS-Kernel/`、`03-Middleware-Drivers/`、`04-API-Guides/`）。
2. **需要新建分类**：若新技术明显属于独立的新领域（例如 `Qt` 属于 UI 框架，`FPGA` 属于可编程逻辑，`ROS` 属于机器人框架），则触发【建文件夹申请机制】。

### 2. 文件夹命名与层级约束
* **层级限制**：新建文件夹必须作为 `01~04` 某个大类下的子目录，**严禁在 `wiki/` 根目录下创建第 6 个同级大类文件夹**（除非人类明确授权）。
* **命名规范**：英文/拼音为主，首字母大写，使用连字符 `-`，禁止特殊字符（例如：`wiki/03-Middleware-Drivers/Qt-GUI/` 或 `wiki/01-Hardware/FPGA-Zynq/`）。

### 3. AI 交互与确认红线（强制阻断）
在创建**任何新的子文件夹**之前，AI 必须在对话窗口中主动暂停，并向人类输出如下格式的申请提案：

> 🚨 **【架构变更申请】发现新技术栈分类**
> 
> * **技术点名称**：[例如：Qt GUI 框架]
> * **拟创建文件夹**：`wiki/03-Middleware-Drivers/Qt-GUI/`
> * **对应的 API 目录**：`wiki/04-API-Guides/Qt-API/`（如需要）
> * **对应的 MOC 索引**：`wiki/00-Index/Qt-知识地图.md`
> * **申请原因**：[简要说明该技术点无法合理归入已有子文件夹的原因]
> 
> ❓ **请回复“同意”或输入你期望的自定义路径以继续操作。**

**⚠️ 禁令**：在未收到人类明确的许可回复（如“同意”、“可以”、“OK”）前，**绝对不许在文件系统中执行任何创建新目录（`mkdir`）的操作**！

