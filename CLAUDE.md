# LLM-Wiki 知识库规范（Obsidian + Codex 专用）

> 基础范式参考 Andrej Karpathy 的 LLM-Wiki 设计，适配中文双链知识库场景

## 核心架构：两层分离（强制遵守）

### 1. `/raw` 原始素材目录【只读、不可修改】

所有导入原文、网页摘录、截图、草稿、原始转录文本，统一存入该目录。 文件一旦存入，禁止 AI 修改、删除、重命名或覆盖；人类仅可新增文件，不得回溯修改已有 raw 文件。

### 2. `/wiki` 结构化知识库【AI 维护区】

存放精炼原子笔记、概念词条、知识汇总内容。 双链关联、内容整合、信息提炼等知识加工操作，均在此目录下完成。

> **分工规则**
> 
> - 人类：仅负责向 raw 目录新增素材，以及最终审核内容。
> - AI（Codex）：仅允许读写 wiki/ 目录，绝对禁止操作 raw/ 目录。

## 一、链接强制规范

- **仅允许使用 Obsidian 双链语法 `[[页面名称]]`**
- 禁止使用普通 Markdown 链接语法 `[文字](链接)` 做库内页面跳转
- 新建页面、补充关联内容时，必须主动建立双向双链

## 二、页面规范

1. **笔记原子化**：单个页面仅描述**单一概念/实体**，禁止制作内容混杂的超长文档
2. 页面标题简洁统一，不使用特殊符号、避免超长中文标题
3. wiki 目录内禁止存放流水账日记、零散随想；随笔草稿统一放入 raw 目录

## 三、内容产出规则

1. 全部输出使用**简体中文**
2. 整理素材时保留信息溯源，必要时标注来源 raw 文件名
3. 内容力求精炼结构化，优先使用列表、分层标题呈现，减少冗余抒情文字
4. 发现新旧信息存在矛盾时，标记冲突并等待人工确认，不得自行选择采信某一方

## 四、AI 操作权限红线【最高优先级】

✅ **允许操作**

- 新增 wiki 页面、补充段落、添加双链、优化排版、拓展相关知识点
- 拆分过长笔记、补全缺失的关联链接、修正笔误

❌ **未经询问绝对禁止**

- 删除任意 wiki 页面
- 批量重命名页面（会破坏已有双链）
- 对已有页面进行全文大规模重写
- 一次性批量创建 10 条以上空白新笔记
- 修改 raw 文件夹内的任何文件
- 擅自删除已存在的标签、人工建立的双向链接

> 执行高危操作前，必须先输出操作方案，等待人工确认后方可执行。

## 五、人工编辑标记规则

若人工手动修改 wiki/ 目录下的任意笔记，需在文档顶部保留如下标记：

> 🧑 人工修改备注：XXX

Codex 识别到此标记后，**不可未经询问直接覆盖人工修改内容**。

## 六、定期知识库自检（Lint 任务）

收到知识库体检指令时，按以下维度检查并整理问题：

1. 孤立页面（无入链的页面）
2. 失效、指向不存在页面的双链
3. 内容互相冲突的词条
4. 体积过大、需要拆分的大笔记
5. 缺少关联链接的相关主题页面

执行大规模整理操作前，必须先输出完整的整理计划，等待确认后执行。

## 七、通用行为准则

1. 修改遵循「微创改动」原则：仅处理当前任务相关内容，不顺手大面积优化无关笔记
2. 不确定意图时主动提问，不自行猜测
3. 所有改动优先采用增量补充方式，优先保留原有有效内容
4. 不擅自新增文件夹、改动知识库目录结构

## 八、wiki/ 目录归档与分类标准

AI 提炼或创建新笔记时，必须严格按照以下规则选定存放路径：

1. **芯片与单片机硬件概念** → 放入 `wiki/01-Hardware/芯片名/`
2. **操作系统、内核机制、线程调度** → 放入 `wiki/02-OS-Kernel/系统名/`
3. **协议栈、音视频库、硬件驱动库原理** → 放入 `wiki/03-Middleware-Drivers/库名/`
4. **API 函数速查、接口文档、代码片段手册** → 放入 `wiki/04-API-Guides/技术名-API/`
5. 新建笔记后，必须检查对应 MOC 文件（如 `wiki/00-Index/xxx-MOC.md`），并将新笔记的双链 `[[页面名称]]` 补充到索引集中。

## 九、新技术栈拓展与受控动态扩容规范（受控拓展协议）

随着知识库扩容，当引入不属于现有四大子分类的技术领域（如 FPGA、Qt、ROS、CUDA 等）时，AI 必须遵循「提案-确认」流程，**严禁自行创建新文件夹**。

### 1. 新技术归类评估标准

收到新素材或创建新笔记指令时，AI 须按顺序评估：

1. **优先归入现有目录**：检查是否可归入现有子文件夹（如 `01-Hardware/`、`02-OS-Kernel/` 等）。
2. **触发新建分类**：若新技术属于独立新领域（例如 Qt 属于 UI 框架、FPGA 属于可编程逻辑、ROS 属于机器人框架），则触发「建文件夹申请机制」。

### 2. 文件夹命名与层级约束

- **层级限制**：新建文件夹必须作为 `01~04` 某个大类下的子目录，**严禁在 wiki/ 根目录下创建第 5 个同级大类文件夹**（除非人工明确授权）。
- **命名规范**：以英文/拼音为主，首字母大写，使用连字符 `-` 分隔，禁止使用特殊字符。例如：`wiki/03-Middleware-Drivers/Qt-GUI/`、`wiki/01-Hardware/FPGA-Zynq/`。

### 3. 申请确认流程（强制阻断）

在创建**任何新的子文件夹**之前，AI 必须暂停操作，并输出如下格式的申请提案：

> 🚨 **【架构变更申请】发现新技术栈分类**
> 
> - **技术点名称**：[例如：Qt GUI 框架]
> - **拟创建文件夹**：`wiki/03-Middleware-Drivers/Qt-GUI/`
> - **对应 API 目录**：`wiki/04-API-Guides/Qt-API/`（如需要）
> - **对应 MOC 索引**：`wiki/00-Index/Qt-知识地图.md`
> - **申请原因**：[简要说明该技术点无法合理归入已有子文件夹的原因]
> 
> ❓ **请回复「同意」或输入自定义路径以继续操作。**

**⚠️ 禁令**：未收到人工明确许可（如「同意」「可以」「OK」）前，**绝对禁止执行任何新建目录操作**。

## 十、API 模块速查手册生成规范（适用 wiki/04-API-Guides/）

提炼或生成 API 手册/函数速查笔记时，禁止写成无代码的纯概念大纲，必须严格包含以下结构：

- **基础头文件**：必须标注该 API 依赖的 C/C++ 对应头文件（如 `#include "lwip/tcp.h"`）
- **核心函数详解**：每个 API 必须包含「作用」「函数原型/签名」「参数含义详解」「返回值状态码」
- **使用 Demo**：每个 API 必须提供 3~8 行精炼的 C/C++ 代码示例
- **避坑红线**：文末必须总结内存泄漏风险、错误码匹配、线程安全或硬件红线等注意事项

## 十一、raw 文件总结记录规范

AI 每完成一次 raw 文件总结，都需要在「源知识库总结记录」笔记中登记，格式如下： `[raw 下被总结的文件名]`：xxxx年xx月xx日，进行了第 x 次总结。

### 十二、非 API 概念/架构类笔记生成规范（深度与视觉化要求）

提炼或生成 `wiki/` 目录下（非 API 手查类）的概念、原理或架构笔记时，**严禁纯文字讲解**，必须遵循以下“原理 + 代码 + 视觉”三维表达标准：

#### 1. 概念与原理部分：必须附带“关键代码/伪代码佐证”

- **禁止纯理论堆砌**：讲解抽象概念（如 FreeRTOS 任务调度、FOC 矢量控制、Linux 字符设备注册、MPP 解码流水线等）时，必须给出 5~10 行**核心原理实现片段**或**伪代码流程**。
    
- **突出关键点**：代码片段中需对关键数据结构（如 `struct` 定义）、状态机转换或寄存器配置增加内联注释。
    

#### 2. 图片与视觉素材引用规范

- **允许且鼓励引用 raw 资源**：若 `raw/` 目录中包含源架构图、电路原理图截图、时序图等图片文件，**必须**在 wiki 笔记中使用 Markdown 语法进行引用（如 `![[raw/assets/mipi-timing.png]]` 或 `![图片描述](../../raw/media/xxx.png)`）。
    
- **图片引用原则**：
    
    1. 仅引用对解释原理有直接帮助的图片，禁止无意义的装饰性图片；
        
    2. 引用图片下方必须附上一句**图注解析**，说明该图重点看什么（如：_图 1：RV1103 MPP 视频流从 MIPI 采集到 H.264 编码的硬件数据流走向_）。
        

#### 3. 笔记基本结构模板

非 API 类型的 wiki 笔记必须至少包含以下四大区块：

1. **概念一句话精炼**（并附带 1-2 个核心双链标签 `[[关联概念]]`）
    
2. **核心原理与图解**（文字 + 引用 raw 目录图片/Diagram）
    
3. **关键实现/数据结构**（精炼代码片段或关键配置）
    
4. **与其他概念的横向对比/关联**（嵌入 Obsidian 双链）